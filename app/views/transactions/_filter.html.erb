<%= form_for :report, url: url, method: :get do |f| %>
  <%= f.date_select :date, { discard_day: true, selected: options.from, include_blank: true } %>
  <%= f.select :account_id, all_accounts, { selected: options.account_id, include_blank: true } %>
  <%= f.submit 'Filter' %>
<%- end %>
